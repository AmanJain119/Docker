var dev = {
    'env': {
        name: 'development',
        server: {
            port: '3000'
        },
        database: {
            name: "demo"
        }
    }
};

config = dev;
module.exports = config;
